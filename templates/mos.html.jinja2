{% extends "base.html.jinja2" %}
{% block heading %}
    <h1 class="display-4">MOS test</h1>
    <p class="lead">Đánh giá chất lượng giọng nói tổng hợp</p>
    <hr class="my-4">
    <p>
        Cảm ơn bạn đã tham gia thí nghiệm này!
    </p>
    <p>
        Trong thí nghiệm này, chúng tôi sử dụng máy tính để tổng hợp giọng nói của con người.
        Tệp âm thanh sau đây chứa các bản ghi âm thực và giọng nói tổng hợp, đồng thời mỗi câu đều có kèm theo bản ghi.
        Yêu cầu các đối tượng nghe bằng tai nghe và đưa ra các đánh giá sau dựa trên chất lượng của từng tệp âm thanh:        <ul>
            <li>1 (rất tệ)</li>
            <li>2 (không tốt)</li>
            <li>3 (bình thường)</li>
            <li>4 (tốt)</li>
            <li>5 (rất tốt)</li>
        </ul>
    </p>
{% endblock %}
{% block form_questions %}
    {% for question in questions %}
        <div class="card form-group">
            <div class="card-header">{{ question.title }}</div>
            <div class="card-body">
                <p>
                    <audio controls src="{{ question.audio_path }}">
                        Your browser does not support the audio element.
                    </audio>
                </p>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="{{ question.name }}" id="{{ question.name }}_1" value="5" required>
                    <label class="form-check-label" for="{{ question.name }}_1">5 (rất tốt)</label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="{{ question.name }}" id="{{ question.name }}_2" value="4" required>
                    <label class="form-check-label" for="{{ question.name }}_2">4 (tốt)</label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="{{ question.name }}" id="{{ question.name }}_3" value="3" required>
                    <label class="form-check-label" for="{{ question.name }}_3">3 (bình thường)</label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="{{ question.name }}" id="{{ question.name }}_4" value="2" required>
                    <label class="form-check-label" for="{{ question.name }}_4">2 (không tốt)</label>
                </div>
                <div class="form-check">
                    <input class="form-check-input" type="radio" name="{{ question.name }}" id="{{ question.name }}_5" value="1" required>
                    <label class="form-check-label" for="{{ question.name }}_5">1 (rất tệ)</label>
                </div>
            </div>
        </div>
    {% endfor %}
{% endblock %}